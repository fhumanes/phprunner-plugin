/**
 * Example for view control control class
 */
Runner.viewControls.ViewTagEditor = Runner.extend(Runner.viewControls.ViewControl,{
	
	/**
	 * Override constructor
	 * @param {Object} cfg
	 */
	constructor: function(cfg){		
		// call parent
		Runner.viewControls.ViewTagEditor.superclass.constructor.call(this, cfg);
	},


	init: function(){

	
	}
});