/**
 * Example for view control control class
 */
Runner.viewControls.ViewBarCode = Runner.extend( Runner.viewControls.ViewControl,{
	/**
	 * Override constructor
	 * @param {Object} cfg
	 */
	constructor: function(cfg) {		
		// call parent
		Runner.viewControls.ViewBarCode.superclass.constructor.call(this, cfg);
		
	},

	init: function() {
	}
});